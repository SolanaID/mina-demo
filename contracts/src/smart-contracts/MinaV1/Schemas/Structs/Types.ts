export enum AvailableNetworks {
    MINA,
    SOLANA,
    POLYGON
}


export enum LinkStatus {
    NULL,
    ACTIVE,
    REVOKED
}

export enum TrustStatus {
    NULL,
    TRUSTED,
    UNTRUSTED
}

export enum ClaimStatus {
    NULL,
    CLAIMED,
    UNCLAIMED,
}